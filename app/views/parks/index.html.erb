<p id="notice"><%= notice %></p>

<h2>Parks</h2>

<table>
  <thead>
    <tr>
      <th>Park Number</th>
      <th>Park Name</th>
      <th>Location</th>
      <th>Number of employees</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @parks.each do |park| %>
      <tr>
        <td><%= park.pno %></td>
        <td><%= park.pname %></td>
        <td><%= park.location %></td>
        <td><%= park.employees %></td>
        <td><%= link_to 'Show', park %></td>
        <td><%= link_to 'Edit', edit_park_path(park) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Park', new_park_path %>
