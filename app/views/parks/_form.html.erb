<%= form_with(model: park, local: true) do |form| %>
  <% if park.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(park.errors.count, "error") %> prohibited this park from being saved:</h2>

      <ul>
      <% park.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pno %>
    <%= form.number_field :pno %>
  </div>

  <div class="field">
    <%= form.label :pname %>
    <%= form.text_field :pname %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="field">
    <%= form.label :employees %>
    <%= form.number_field :employees %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
